 #  Bar Plot (Top N): Top 10 Release Group by $Worldwide revenue
     #Plot 6
    top_10_release_groups = df.groupby('Release Group')['$Worldwide'].sum().nlargest(10).sort_values(ascending=False)
    plt.figure(figsize=(12, 7))
    sns.barplot(x=top_10_release_groups.values, y=top_10_release_groups.index, palette='viridis')
    plt.title('Top 10 Release Groups by Worldwide Revenue')
    plt.xlabel('Worldwide Revenue ($)')
    plt.ylabel('Release Group')
    plt.tight_layout()
    plt.show()